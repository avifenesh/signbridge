[project]
name = "asl-engine"
version = "0.1.0"
description = "SignBridge Agent 4 — ASL Translation Engine"
requires-python = ">=3.11"

[project.optional-dependencies]
dev = ["pytest>=8.0"]
onnx = ["onnxruntime>=1.17", "numpy>=1.26"]

[project.scripts]
translate = "cli:main"
